package com.elp.service;
import com.elp.model.Atencion;
import java.util.List;

public interface IAtencionService extends ICRUD<Atencion, Integer> {
    /*
    Atencion save(Atencion atencion);
    Atencion update(Integer id, Atencion atencion);
    List<Atencion> findAll();
    Atencion findById(Integer id);
    void delete(Integer id);
     */
}
